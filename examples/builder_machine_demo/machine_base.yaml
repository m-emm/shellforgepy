ShellforgepyBuilderVersion: "2026-03-27"

Description: Base plate with named interfaces for the two gantry uprights

Parameters:
  length:
    Type: Float
  depth:
    Type: Float
  thickness:
    Type: Float
  profile_size:
    Type: Float
  upright_inset:
    Type: Float

Parts:
  Base:
    Type: Shellforgepy::Assembly
    Properties:
      Generator: examples.builder_machine_demo.machine_base_generator.create_machine_base
      Properties:
        length:
          $ref: length
        depth:
          $ref: depth
        thickness:
          $ref: thickness
        profile_size:
          $ref: profile_size
        upright_inset:
          $ref: upright_inset
