ShellforgepyBuilderVersion: "2026-03-27"

Description: Reusable rectangular structural member

Parameters:
  length:
    Type: Float
  depth:
    Type: Float
  height:
    Type: Float

Parts:
  StructuralMember:
    Type: Shellforgepy::Assembly
    Properties:
      Generator: shellforgepy.simple.create_box
      Properties:
        length:
          $ref: length
        width:
          $ref: depth
        height:
          $ref: height
